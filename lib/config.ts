/**
 * Site-wide feature flags.
 *
 * ADS_ENABLED: set to `true` once AdSense (or another ad network) has been
 * approved for this site. While `false`, ad slots are not rendered anywhere
 * so no empty/placeholder ad boxes are shown to visitors.
 */
export const ADS_ENABLED = false;
